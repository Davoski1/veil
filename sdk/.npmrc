# The SDK dev-depends on react@^18 while some tooling in the tree resolves
# react-dom@19, a pre-existing peer mismatch on main. Allow installs to proceed
# so `npm ci` (CI, incl. the SDK Browser Matrix job) resolves deterministically.
legacy-peer-deps=true
